# 跨平台 CLI 工具 (Cobra)

本项目是Go实战入门路线的第四阶段，目标是掌握Go的构建能力，Go最擅长写CLI工具。

## 项目目标

- CLI 主命令 + 多子命令
- 自动生成 `--help`
- 支持 YAML 配置
- 统一 logging
- 一键编译跨平台二进制（Linux / Mac / Windows）

## 示例命令结构

```
mycli
├── deploy
├── build
└── config
```

## 启动方式

待完善...